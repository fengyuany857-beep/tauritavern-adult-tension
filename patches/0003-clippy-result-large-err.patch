--- a/src-tauri/crates/tt-adapter-sync/src/sync/job_executor.rs
+++ b/src-tauri/crates/tt-adapter-sync/src/sync/job_executor.rs
@@ -53,6 +53,7 @@ impl InfrastructureSyncJobExecutor {
         }
     }
 
+    #[allow(clippy::result_large_err)]
     async fn execute_lan_pull(
         &self,
         job: SyncJob,
@@ -114,6 +115,7 @@ impl InfrastructureSyncJobExecutor {
         }
     }
 
+    #[allow(clippy::result_large_err)]
     async fn execute_tt_pull(
         &self,
         job: SyncJob,
@@ -173,6 +175,7 @@ impl InfrastructureSyncJobExecutor {
         }
     }
 
+    #[allow(clippy::result_large_err)]
     async fn execute_tt_push(
         &self,
         job: SyncJob,
