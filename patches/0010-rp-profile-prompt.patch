--- a/src-tauri/crates/tt-application/src/services/agent_profile_service/defaults.rs
+++ b/src-tauri/crates/tt-application/src/services/agent_profile_service/defaults.rs
@@ -130,8 +130,10 @@
 2. Call the model-facing tool rp_state before drafting. Use rp_recall when older accepted history matters.
 3. Preserve Fixed Canon. Never author the player's dialogue/actions/attempts/feelings/intentions/internal state beyond what the user explicitly supplied. NPCs remain autonomous.
 4. Historical continuity never grants current consent, boundaries, safety or permission. Resolve those only from the current explicit user turn plus the accepted runtime authority returned by rp_state. Current boundary/grant/pause changes belong only in rp_stage_turn.authority, never in continuity facts. Any scene/location/participant change conservatively withdraws previous current grants.
-5. Decide one coherent beat, then call the model-facing tool rp_stage_turn exactly once successfully with only factual consequences that actually occur in the response. For a genuinely non-advancing OOC/meta reply, call rp_stage_turn with advances_story=false and no story/history mutation fields. Non-advancing explicit boundary/grant/pause/resume commands may update authority without advancing the story. Do not put player internal-state facts into continuity facts.
-6. Write the response to output/main.md with workspace_write_file, call workspace_commit, then workspace_finish.
+5. NPC knowledge is source-bound. A newly introduced NPC does not know accepted facts merely because they exist in RP state. Any positive knowledge row must provide source.kind; when a new NPC goal/beat/fact relies on prior continuity, bind the accepted fact id in npc_updates.knowledge_refs and stage matching knowledge.fact_id + source provenance. Use observed only for facts actually perceived, told_by only when the speaker/source exists in the beat, public only for established public information, and user_asserted only when the current user explicitly establishes that knowledge. Inference may create suspects/partial, never knows.
+6. If the current explicit user says an already accepted prior story beat never happened, do not merely mark an event cancelled. Use retract_turns plus retraction_reason, normally with advances_story=false, so facts/knowledge/events/commitments/threads/agendas/recent beats/NPC runtime updates originating in those turns are removed from active continuity. Use facts.supersedes for a targeted fact correction that should preserve the rest of a turn.
+7. Decide one coherent beat, then call the model-facing tool rp_stage_turn exactly once successfully with only factual consequences that actually occur in the response. For a genuinely non-advancing OOC/meta reply, call rp_stage_turn with advances_story=false and no new story/history mutation fields. Pure retract_turns corrections and explicit boundary/grant/pause/resume commands may be non-advancing. Do not put player internal-state facts into continuity facts.
+8. Write the response to output/main.md with workspace_write_file, call workspace_commit, then workspace_finish.
 
 The branch-local RP state becomes durable only through workspace.finish and TauriTavern's persistent-state binding to the current assistant swipe. Regenerate/overswipe therefore create alternate states without contaminating the selected branch."#.to_string()),
         },
