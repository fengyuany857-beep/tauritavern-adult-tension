diff --git a/src-tauri/crates/tauritavern/src/app/data_continuity.rs b/src-tauri/crates/tauritavern/src/app/data_continuity.rs
--- a/src-tauri/crates/tauritavern/src/app/data_continuity.rs
+++ b/src-tauri/crates/tauritavern/src/app/data_continuity.rs
@@ -205,7 +205,9 @@ fn create_internal_recovery_snapshot(
         created_at: &timestamp,
         bundle_identifier: EXPECTED_BUNDLE_IDENTIFIER,
         source_app_version: crate::product::VERSION,
-        source_build_revision: crate::product::GIT_REVISION.unwrap_or("unknown"),
+        source_build_revision: crate::product::optional_build_value(crate::product::GIT_REVISION)
+            .unwrap_or("unknown"),
         data_schema_version: DATA_SCHEMA_VERSION,
         adult_tension_suite_version: "adult-tension-runtime-skills-v1",
         include_secrets: true,
@@ -338,8 +340,8 @@ fn marker_path(data_root: &Path) -> PathBuf {
 }
 
 fn current_build_identity() -> String {
-    match crate::product::GIT_REVISION {
-        Some(revision) if !revision.trim().is_empty() => {
+    match crate::product::optional_build_value(crate::product::GIT_REVISION) {
+        Some(revision) => {
             format!("{}+{}", crate::product::VERSION, revision)
         }
         _ => crate::product::VERSION.to_string(),
