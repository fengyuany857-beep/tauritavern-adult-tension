diff --git a/src-tauri/crates/tt-application/src/services/character_card_materialization_orchestrator.rs b/src-tauri/crates/tt-application/src/services/character_card_materialization_orchestrator.rs
--- a/src-tauri/crates/tt-application/src/services/character_card_materialization_orchestrator.rs
+++ b/src-tauri/crates/tt-application/src/services/character_card_materialization_orchestrator.rs
@@ -82,6 +82,7 @@ pub struct CharacterCardReturnPlanResult {
     pub plan: ReturnImportPlan,
     pub resolved_definition: Value,
     pub avatar_may_replace: bool,
+    pub replace_avatar: bool,
 }
 
 pub struct CharacterCardMaterializationOrchestrator {
@@ -406,10 +407,16 @@ impl CharacterCardMaterializationOrchestrator {
         &self,
         staged_ref: &str,
         format: PortableCharacterCardFormat,
+        replace_avatar: bool,
         expected_session_id: &SessionId,
         expected_branch_id: &BranchId,
         choices: &BTreeMap<String, ReturnFieldChoice>,
     ) -> Result<CharacterCardReturnPlanResult, DomainError> {
+        if format == PortableCharacterCardFormat::Json && replace_avatar {
+            return Err(DomainError::InvalidData(
+                "JSON Return cannot replace avatar pixels".to_string(),
+            ));
+        }
         let staged_bytes = self.materialization.read_staged_import(staged_ref).await?;
@@ -527,6 +534,8 @@ impl CharacterCardMaterializationOrchestrator {
             &plan,
             expected_session_id,
             expected_branch_id,
+            format,
+            replace_avatar,
         );
         plan.validate_shape().map_err(DomainError::InvalidData)?;
 
@@ -535,6 +544,7 @@ impl CharacterCardMaterializationOrchestrator {
             plan,
             resolved_definition,
             avatar_may_replace: format == PortableCharacterCardFormat::Png,
+            replace_avatar,
         })
     }
diff --git a/src-tauri/crates/tt-application/src/services/character_card_materialization_orchestrator.rs b/src-tauri/crates/tt-application/src/services/character_card_materialization_orchestrator.rs
--- a/src-tauri/crates/tt-application/src/services/character_card_materialization_orchestrator.rs
+++ b/src-tauri/crates/tt-application/src/services/character_card_materialization_orchestrator.rs
@@ -696,29 +706,25 @@ fn resolve_return_decisions(
 ) -> Result<Value, DomainError> {
     let mut resolved = local.clone();
     for decision in decisions {
-        let value = if let Some(value) = decision.resolution.clone() {
-            value
+        let value = if let Some(value) = decision.resolution.clone() {
+            Some(value)
         } else {
             match choices.get(&decision.field_path) {
-                Some(ReturnFieldChoice::KeepLocal) => decision.local.clone().ok_or_else(|| {
-                    DomainError::InvalidData(format!(
-                        "field {} has no local value; implicit deletion is not allowed",
-                        decision.field_path
-                    ))
-                })?,
-                Some(ReturnFieldChoice::UseImported) => decision.imported.clone().ok_or_else(|| {
-                    DomainError::InvalidData(format!(
-                        "field {} is missing from imported card; missing is not deletion",
-                        decision.field_path
-                    ))
-                })?,
+                // Explicit KeepLocal may preserve an already-absent LOCAL field.
+                // This is not an imported deletion: the local absence already
+                // existed before Return planning.
+                Some(ReturnFieldChoice::KeepLocal) => decision.local.clone(),
+                Some(ReturnFieldChoice::UseImported) => Some(
+                    decision.imported.clone().ok_or_else(|| {
+                        DomainError::InvalidData(format!(
+                            "field {} is missing from imported card; missing is not deletion",
+                            decision.field_path
+                        ))
+                    })?,
+                ),
                 None => {
                     return Err(DomainError::Conflict(format!(
                         "field {} requires an explicit merge choice",
@@ -728,7 +734,7 @@ fn resolve_return_decisions(
                 }
             }
         };
-        set_return_field(&mut resolved, &decision.field_path, value)?;
+        set_return_field(&mut resolved, &decision.field_path, value)?;
     }
@@ -744,7 +750,11 @@ fn resolve_return_decisions(
     Ok(resolved)
 }
 
-fn set_return_field(target: &mut Value, field_path: &str, value: Value) -> Result<(), DomainError> {
+fn set_return_field(
+    target: &mut Value,
+    field_path: &str,
+    value: Option<Value>,
+) -> Result<(), DomainError> {
     let root = target.as_object_mut().ok_or_else(|| {
         DomainError::InvalidData("resolved Character Card root must be an object".to_string())
     })?;
@@ -757,7 +767,11 @@ fn set_return_field(target: &mut Value, field_path: &str, value: Value) -> Resu
                 "resolved Character Card data must be an object".to_string(),
             ))?;
-        data.insert(data_key.to_string(), value);
+        if let Some(value) = value {
+            data.insert(data_key.to_string(), value);
+        } else {
+            data.remove(data_key);
+        }
         return Ok(());
     }
@@ -767,7 +781,11 @@ fn set_return_field(target: &mut Value, field_path: &str, value: Value) -> Resu
             "unsupported Character Card merge field path {field_path}"
         )));
     }
-    root.insert(field_path.to_string(), value);
+    if let Some(value) = value {
+        root.insert(field_path.to_string(), value);
+    } else {
+        root.remove(field_path);
+    }
     Ok(())
 }
@@ -778,6 +796,8 @@ pub(crate) fn compute_return_plan_token(
     plan: &ReturnImportPlan,
     session_id: &SessionId,
     branch_id: &BranchId,
+    format: PortableCharacterCardFormat,
+    replace_avatar: bool,
 ) -> String {
@@ -797,6 +817,8 @@ pub(crate) fn compute_return_plan_token(
         plan.resolved_choices_hash.as_str(),
         session_id.0.as_str(),
         branch_id.0.as_str(),
+        format.as_str(),
+        if replace_avatar { "replace_avatar" } else { "preserve_avatar" },
     ] {
