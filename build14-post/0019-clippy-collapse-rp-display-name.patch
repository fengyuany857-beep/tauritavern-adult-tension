diff --git a/src-tauri/crates/tt-application/src/services/agent_tools/rp/state.rs b/src-tauri/crates/tt-application/src/services/agent_tools/rp/state.rs
--- a/src-tauri/crates/tt-application/src/services/agent_tools/rp/state.rs
+++ b/src-tauri/crates/tt-application/src/services/agent_tools/rp/state.rs
@@ -1407,9 +1407,9 @@ fn apply_cast_update(state: &mut RpState, update: &CastUpdateInput, turn: u64) -> Result<(), ApplicationError> {
-        if let Some(name) = update.display_name.as_deref().map(str::trim).filter(|value| !value.is_empty()) {
-            if name != character.display_name {
-                if !character.aliases.contains(&character.display_name) { character.aliases.push(character.display_name.clone()); }
-                character.display_name = name.to_string();
-            }
+        if let Some(name) = update.display_name.as_deref().map(str::trim).filter(|value| !value.is_empty())
+            && name != character.display_name
+        {
+            if !character.aliases.contains(&character.display_name) { character.aliases.push(character.display_name.clone()); }
+            character.display_name = name.to_string();
         }
         if let Some(aliases) = update.aliases.as_ref() { character.aliases = dedupe(aliases.clone()); }
