diff --git a/src-tauri/crates/tauritavern/src/presentation/commands/character_commands.rs b/src-tauri/crates/tauritavern/src/presentation/commands/character_commands.rs
--- a/src-tauri/crates/tauritavern/src/presentation/commands/character_commands.rs
+++ b/src-tauri/crates/tauritavern/src/presentation/commands/character_commands.rs
@@ -4,8 +4,9 @@ use std::sync::Arc;
 use tauri::State;
 use tt_application::services::character_card_draft_materialization_service::{
-    CharacterCardDraftCommitRequest, CharacterCardDraftCommitResult,
-    CharacterCardDraftPreview,
+    CharacterCardDraftCandidateList, CharacterCardDraftCommitRequest,
+    CharacterCardDraftCommitResult, CharacterCardDraftPreview,
 };
 use tt_application::services::character_card_materialization_orchestrator::{
     CharacterCardReturnCommitResult, CharacterCardReturnContextResult,
@@ -293,6 +294,21 @@ pub async fn export_character_content(
         .map_err(map_command_error("Failed to export character content"))
 }
 
+#[tauri::command]
+pub async fn list_character_card_draft_candidates(
+    stable_chat_id: String,
+    app_state: State<'_, Arc<AppState>>,
+) -> Result<CharacterCardDraftCandidateList, CommandError> {
+    log_command("list_character_card_draft_candidates");
+    app_state
+        .services
+        .character_card_draft_materialization_service
+        .list_candidates(&stable_chat_id)
+        .await
+        .map_err(map_command_error(
+            "Failed to list Character Card draft candidates",
+        ))
+}
+
 #[tauri::command]
 pub async fn preview_character_card_draft(
     stable_chat_id: String,
diff --git a/src-tauri/crates/tauritavern/src/presentation/commands/registry.rs b/src-tauri/crates/tauritavern/src/presentation/commands/registry.rs
--- a/src-tauri/crates/tauritavern/src/presentation/commands/registry.rs
+++ b/src-tauri/crates/tauritavern/src/presentation/commands/registry.rs
@@ -26,6 +26,7 @@ pub fn invoke_handler() -> impl Fn(tauri::ipc::Invoke<tauri::Wry>) -> bool + Send + Sync + 'static {
         super::character_commands::replace_character,
         super::character_commands::export_character,
         super::character_commands::export_character_content,
+        super::character_commands::list_character_card_draft_candidates,
         super::character_commands::preview_character_card_draft,
         super::character_commands::commit_character_card_draft,
