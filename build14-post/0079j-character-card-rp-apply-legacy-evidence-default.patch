diff --git a/src-tauri/crates/tt-application/src/services/character_card_rp_apply_service.rs b/src-tauri/crates/tt-application/src/services/character_card_rp_apply_service.rs
--- a/src-tauri/crates/tt-application/src/services/character_card_rp_apply_service.rs
+++ b/src-tauri/crates/tt-application/src/services/character_card_rp_apply_service.rs
@@ -626,9 +626,14 @@ fn apply_profile_selections(
     let profile_revision = current_revision.saturating_add(1);
     entity.insert("profileRevision".to_string(), json!(profile_revision));
 
-    let evidence = state
-        .get_mut("profileEvidence")
-        .and_then(Value::as_array_mut)
+    let evidence_value = state
+        .as_object_mut()
+        .ok_or_else(|| ApplicationError::ValidationError(
+            "CARD_RP_STATE_INVALID: RP state root must be an object".to_string(),
+        ))?
+        .entry("profileEvidence")
+        .or_insert_with(|| json!([]));
+    let evidence = evidence_value.as_array_mut()
         .ok_or_else(|| ApplicationError::ValidationError(
             "CARD_RP_STATE_INVALID: profileEvidence must be an array".to_string(),
         ))?;
