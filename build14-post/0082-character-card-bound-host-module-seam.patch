diff --git a/src/scripts/extensions/agent-system/src/adult-tension-cast-durable.js b/src/scripts/extensions/agent-system/src/adult-tension-cast-durable.js
--- a/src/scripts/extensions/agent-system/src/adult-tension-cast-durable.js
+++ b/src/scripts/extensions/agent-system/src/adult-tension-cast-durable.js
@@ -1,2 +1,1 @@
-import { characters, createOrEditCharacter, select_selected_character } from '../../../../script.js';
 import { getActiveChatSnapshot } from '../../../../tauri/main/adapters/st/active-chat-ref.js';
@@ -98,4 +98,5 @@ async function openBoundCard(candidate) {
     const storageIdentity = candidate?.binding?.cardStorageIdentity;
     if (!storageIdentity) throw new Error(tr('boundCardNotFound'));
+    const { characters, createOrEditCharacter, select_selected_character } = await import('../../../../script.js' /* webpackIgnore: true */);
     const index = characters.findIndex(character => avatarStorageIdentity(character?.avatar) === storageIdentity);
     if (index < 0) throw new Error(tr('boundCardNotFound'));
diff --git a/tests/character-card-bound-management-contract.test.mjs b/tests/character-card-bound-management-contract.test.mjs
--- a/tests/character-card-bound-management-contract.test.mjs
+++ b/tests/character-card-bound-management-contract.test.mjs
@@ -37,8 +37,9 @@ test('only an active Binding is treated as an already materialized Card', () => {
 });
 
 test('bound RP people navigate into the existing Character Library editor', () => {
     const ui = read('src/scripts/extensions/agent-system/src/adult-tension-cast-durable.js');
-    assert.match(ui, /characters, createOrEditCharacter, select_selected_character/);
+    assert.match(ui, /import\('\.\.\/\.\.\/\.\.\/\.\.\/script\.js' \/\* webpackIgnore: true \*\/\)/);
+    assert.doesNotMatch(ui, /from ['"][^'"]*script\.js['"]/);
     assert.match(ui, /function openBoundCard\(candidate\)/);
     assert.match(ui, /candidate\?\.binding\?\.cardStorageIdentity/);
     assert.match(ui, /characters\.findIndex/);
